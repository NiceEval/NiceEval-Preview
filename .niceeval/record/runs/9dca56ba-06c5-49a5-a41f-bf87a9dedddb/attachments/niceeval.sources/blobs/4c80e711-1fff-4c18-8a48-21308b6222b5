import { defineEval } from "niceeval";

export default defineEval({
  description: "A deliberate explicit skip yielding skipped",
  tags: ["state", "skipped"],
  test(t) {
    t.skip("DELIBERATE_EXPECTED_SKIP_MARKER: preview skipped state");
  },
});
