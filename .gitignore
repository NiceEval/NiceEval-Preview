node_modules/
.niceeval/
.niceeval-preview-dogfood-*/
dist/
coverage/

# Fixed View consumes a CLI-created, sealed RecordSnapshot. The dogfood script
# writes both it and temporary versioned query requests outside this checkout.

# pnpm dev:link may leave machine-local diagnostics; its workspace override and
# lockfile changes must be restored before committing.
*.tgz
*.log
.DS_Store
